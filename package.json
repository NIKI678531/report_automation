{
  "name": "monthly-commentary-platform",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @commentary/web",
    "build": "npm run build --workspace @commentary/web",
    "test": "npm run test --workspace @commentary/web"
  }
}
